# Mini CRM

CRM ligero en HTML/CSS/JavaScript para gestionar:

- Contactos
- Oportunidades
- Tareas

## Ejecutar

Abre `index.html` en el navegador.

## Persistencia

Los datos se guardan automáticamente en `localStorage` con la clave `mini-crm-db`.
